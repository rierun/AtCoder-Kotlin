@kotlin.ExperimentalStdlibApi
fun main() {
    // 事前準備 (答えや関数等)
    var ans = "0"

    // 入力
    val n = readln().toByte()
    val a = byteArrayOf(n)

    // 処理
    ans = String(a)


    // 出力
    println(ans)
}