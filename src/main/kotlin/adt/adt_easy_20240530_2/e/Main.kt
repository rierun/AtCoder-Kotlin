@kotlin.ExperimentalStdlibApi
fun main() {
    // 事前準備 (答えや関数等)
    var ans = 0
    val a = List<Long> (9) { 1 }

    // 入力
    val n = readln().toInt()

    // 処理
    for (i in 1 until n) {
        for (j in 1 until 9) {
            
        }
    }

    // 出力
    println(ans)
}