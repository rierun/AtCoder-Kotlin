@kotlin.ExperimentalStdlibApi
fun main() {
    val n = readln().toInt()
    val s = List (n) { readln() }

    // 処理
    var ans = -1
    for (i in 0..9) {
        for (j in s) {
            val x = j.split("")
        }
    }


    println()
}